import { configApi } from '../api/config.client';
export const getConfig = configApi.getConfig;
export const getRentalTimeOptions = configApi.getRentalTimeOptions;
export const getExtendTimeOptions = configApi.getExtendTimeOptions;
